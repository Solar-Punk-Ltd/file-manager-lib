// Stub used by the `unit-browser` Jest project to force all `isNode` checks to return false, exercising browser-specific code paths
export const isNode = false;
export const isBrowser = true;
export const isWebWorker = false;
export const isDeno = false;
export const isCI = false;
